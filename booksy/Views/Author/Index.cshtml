@model IEnumerable<Booksy.Models.Author>

@{
    ViewData["Title"] = "Authors";
}

<div class="authors-container" style="margin-top: 100px;">
    <h1>Authors</h1>

    <p class="create-author-btn">
        <a asp-controller="Author" asp-action="Create" class="btn btn-primary">Create New Author</a>
    </p>

    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead class="table-dark">
                <tr>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Biography</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.FirstName</td>
                        <td>@item.LastName</td>
                        <td>@item.Biography</td>
                        <td>
                            <a asp-controller="Author" asp-action="Edit" asp-route-id="@item.AuthorId" class="btn btn-success"> <i class="fas fa-pen"></i></a>
                            <a asp-controller="Author" asp-action="Details" asp-route-id="@item.AuthorId" class="btn btn-secondary"> <i class="fas fa-info-circle"></i></a>
                            <a asp-controller="Author" asp-action="Delete" asp-route-id="@item.AuthorId" class="btn btn-danger"> <i class="fas fa-trash"></i></a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

